---
import { getWhatsappUrl } from "src/utils/whatsapp"
---

<section class="hero-container max-h-[800px] h-screen min-h-[600px] overflow-hidden relative">
	<div class="parallax-bg" id="parallax-bg"></div>
	<div class="building-image" id="building-image"></div>

	<!-- Floating decorative elements -->
	<div class="floating-element left-10 top-1/4 opacity-20">
		<div class="w-16 h-16 rounded-full bg-dark-orange"></div>
	</div>

	<div class="container h-full">
		<div class="hero-content">
			<div class="max-w-2xl text-white md:ml-12 hero-text-container">
				<h1 class="text-4xl md:text-6xl font-bold mb-6 md:leading-16">
					Encuentra tu <span class="text-dark-orange">inmueble ideal</span> con Eiich Inmobiliaria
				</h1>
				<p class="mb-10 opacity-90 max-w-xl">
					En Eiich Inmobiliaria, te damos la bienvenida a un espacio donde tus sueños y proyectos toman forma.
					Nos especializamos en la compra y venta de departamentos, terrenos, locales comerciales y más,
					ofreciendo un servicio personalizado, confiable y transparente.
				</p>

				<div class="flex flex-col sm:flex-row gap-4 justify-start">
					<a
						href="#featured"
						class="bg-dark-orange hover:bg-[#e6732b] text-white px-8 py-4 rounded-lg font-bold transition transform hover:scale-105 shadow-lg flex items-center justify-center"
					>
						<i class="fas fa-search mr-2"></i> Buscar propiedades
					</a>
					<a
						href={getWhatsappUrl()}
						target="_blank"
						rel="noopener noreferrer"
						class="bg-transparent border-2 border-white hover:bg-white hover:text-midnight-blue text-white px-8 py-4 rounded-lg font-bold transition transform hover:scale-105 flex items-center justify-center"
					>
						<i class="fas fa-phone mr-2"></i> Contactar asesor
					</a>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.hero-container {
		position: relative;
	}

	.parallax-bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-image: url("/src/assets/images/fondo.png");
		background-size: cover;
		background-position: center;
		will-change: transform;
		z-index: -2;
	}

	.building-image {
		position: absolute;
		right: 0;
		top: 0;
		width: 50%;
		height: 100%;
		background-image: url("/src/assets/images/edificio.png");
		background-size: cover;
		background-position: center;
		transform: perspective(10px) rotateX(0deg);
		transform-origin: right center;
		will-change: transform;
		z-index: -1;
		opacity: 0.8;
		clip-path: polygon(20% 0%, 100% 0%, 100% 100%, 0% 100%);
	}

	.hero-content {
		position: relative;
		z-index: 1;
		height: 100%;
		display: flex;
		align-items: center;
		justify-content: flex-start;
		text-align: left;
	}

	.hero-text-container {
		margin-top: 0;
		transform: translateY(0);
	}

	.floating-element {
		position: absolute;
		animation: float 6s ease-in-out infinite;
	}

	@keyframes float {
		0% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-20px);
		}
		100% {
			transform: translateY(0px);
		}
	}

	@media (max-width: 768px) {
		.building-image {
			width: 100%;
			transform: perspective(1000px) rotateX(20deg);
			opacity: 0.6;
			clip-path: none;
		}

		.hero-content {
			padding-top: 0;
			justify-content: center;
			text-align: left;
		}

		.hero-text-container {
			background-color: rgba(12, 38, 71, 0.7);
			padding: 2rem;
			border-radius: 1rem;
			margin: 0 auto;
		}
	}
</style>

<script>
	// Enhanced parallax effect usando Alpine.js data
	document.addEventListener("DOMContentLoaded", function () {
		// Smooth scrolling for anchor links
		// document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
		// 	anchor.addEventListener("click", function (e) {
		// 		e.preventDefault()

		// 		const targetId = this.getAttribute("href")
		// 		if (targetId === "#") return

		// 		const targetElement = document.querySelector(targetId)
		// 		if (targetElement) {
		// 			targetElement.scrollIntoView({
		// 				behavior: "smooth",
		// 			})
		// 		}
		// 	})
		// })

		// Enhanced parallax effect
		document.addEventListener("mousemove", function (e) {
			const x = e.clientX / window.innerWidth
			const y = e.clientY / window.innerHeight

			const bg = document.getElementById("parallax-bg")
			const building = document.getElementById("building-image")

			if (bg && building) {
				// More subtle background movement
				// bg.style.transform = `translate(${x * 10 - 5}px, ${y * 10 - 5}px)`
				// Stronger building perspective effect
				// building.style.transform = `perspective(1000px) rotateX(${15 + y * 10}deg) rotateY(${x * 4 - 2}deg)`
			}
		})
	})
</script>
